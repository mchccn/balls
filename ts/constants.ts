const SIZE = 25;
const GRAVITY = 1;
const FPS = 60;
const WIDTH = 1600;
const HEIGHT = 900;
const WORLD_WIDTH = WIDTH * 20;
const WORLD_HEIGHT = HEIGHT * 10;

export { SIZE, GRAVITY, FPS, WIDTH, HEIGHT, WORLD_WIDTH, WORLD_HEIGHT };
